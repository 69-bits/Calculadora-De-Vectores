#pragma once // Preprocessor directive to ensure this header is included only once.
#include <iostream> // Include the C++ Standard Input/Output library.
#include <string>  // Include the C++ Standard String library.
using namespace std; // Import the 'std' namespace to use C++ standard library symbols.